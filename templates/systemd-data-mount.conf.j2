[Unit]
Description={{ mount.description }}

[Mount]
What=/dev/disk/by-uuid/{{ mount.uuid }}
Where=/mnt/{{ mount.name }}
Type={{ mount.fstype }}
Options={{ mount.options }}

[Install]
WantedBy=multi-user.target
